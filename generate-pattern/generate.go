package gen

import "image"

func GenerateExcelPattern(image image.Image, fileName, authorScreenName string) string {
	path := "./patterns/" + fileName

	// Generate Pattern (w/ list of thread colors)
	// and save it to path, then return path

	return path
}
